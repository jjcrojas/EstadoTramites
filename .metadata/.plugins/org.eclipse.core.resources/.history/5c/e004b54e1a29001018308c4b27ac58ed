<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Actividades Vigentes</title>
    <style>
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 1em;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            font-size: 0.9em;
        }
        th {
            background-color: #f2f2f2;
        }
        body {
            font-family: Arial, sans-serif;
            margin: 2em;
        }
    </style>
</head>
<body>
    <h2>Actividades Vigentes</h2>
    <table>
        <thead>
            <tr>
                <th>Expediente</th>
                <th>Código Trámite</th>
                <th>Nombre Trámite</th>
                <th>Fecha Radicado Cero</th>
                <th>Fecha Último Derivado</th>
                <th>Descripción Último Derivado</th>
                <th>Nombre Actividad</th>
                <th>Fecha Creación Actividad</th>
                <th>Responsable Actividad</th>
                <th>Días desde Radicado</th>
            </tr>
        </thead>
        <tbody>
            <tr th:each="fila : ${actividades}">
                <td th:text="' ' + ${item['EXPEDIENTE']}"></td>
                <td th:text="${fila.codigo_tramite}">Cod</td>
                <td th:text="${fila.nombre_tramite}">Trámite</td>
                <td th:text="${fila.fecha_radicado_cero}">Fecha</td>
                <td th:text="${fila.fecha_ultimo_derivado}">Fecha Derivado</td>
                <td th:text="${fila.descripcion_ultimo_derivado}">Descripción</td>
                <td th:text="${fila.nombre_actividad}">Actividad</td>
                <td th:text="${fila.fecha_creacion_actividad}">Fecha Act.</td>
                <td th:text="${fila.responsable_actividad}">Responsable</td>
                <td th:text="${fila.dias_desde_radicado}">Días</td>
            </tr>
        </tbody>
    </table>
</body>
</html>
